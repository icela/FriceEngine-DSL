apply plugin: 'java'
apply plugin: 'kotlin'

sourceSets {
	main.kotlin.srcDirs += 'src'
	main.java.srcDirs += 'src'
	test.kotlin.srcDirs += 'test'
	test.java.srcDirs += 'test'
}

dependencies {
	compile 'dsl'
}